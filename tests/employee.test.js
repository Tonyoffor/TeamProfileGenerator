const Employee = require("../lib/Employee");
describe('Employee', () => {
    it('This will display the Employee information', () => {
      expect(new Employee(

        
      )).toBe(
        
      );
    })})